import Container from "../ui/Container";
import WorkoutsList from "../features/workout/WorkoutsList";

const Home = () => {
  return (
    <Container>
      <WorkoutsList />
    </Container>
  );
};

export default Home;
